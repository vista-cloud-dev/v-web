---
name: m6.4-vweb-fhir-patient
description: VSL/MSL M6.4 DONE — VWEBR router + FHIR R4 GET /Patient/:id handler (the first real route on the M6.3 transport), dual-engine green incl. a live #2 read on vehu.
metadata:
  type: project
---

VSL/MSL **M6.4 DONE** (2026-06-17), branch `m6.4-fhir-patient` off `main`. The
first real route mounts on the M6.3 transport: a minimal FHIR R4 `Patient` read
from PATIENT (#2) through FileMan DBS. New **`VWEBR`** + suite **`VWEBRTST`**;
`VWEBL` refactored.

## What shipped
- **`routes^VWEBR(.SRV)`** — the route table, now VWEBR-owned (STDHTTPD §6 Q3
  caller-owned data). Registers `GET /healthz` → `health^VWEBL` (kept) **and**
  `GET /Patient/:id` → `getPatient^VWEBR`. `healthRoutes^VWEBL` was **removed**;
  `VWEBL.run` + every VWEBLTST setup now call `routes^VWEBR`. (Route-table
  *store* — spec D3, FileMan-file-backed vs code-built — stays OUT of scope;
  table is code-built; **D3 owed**.)
- **`getPatient^VWEBR(.REQ,.RSP)`** — STDHTTPD handler signature. DFN from
  `REQ("param","id")`. **non-numeric id → 400**, **absent DFN → 404**, else
  **200** + a minimal FHIR R4 Patient. Auth-agnostic (M6.5 adds auth).
- **FHIR mapping** (cited in the routine header): `id`←DFN; `identifier`←DFN as
  an **MRN** (type code `MR`, system `urn:vista:dfn`); `name.family/given`←#.01
  NAME ("LAST,FIRST MIDDLE"); `gender`←#.02 SEX (FileMan external MALE/FEMALE →
  male/female); `birthDate`←#.03 DOB (FileMan external "JAN 01, 1950" → FHIR
  `YYYY[-MM[-DD]]`). 400/404 emit a FHIR `OperationOutcome`.

## Non-obvious decisions / gotchas
- **JSON via the `RSP("json")` seam, NOT a hand `$$encode^STDJSON` call.** The
  handler builds the STDJSON typed-node tree (`"o"`/`"a"`/`"s:"`) directly in
  `RSP("json")`; `serializeRsp^STDHTTPMSG` does `merge jnode=RSP("json")` (a
  top-level copy — this *is* the M6.1 IRIS "subscripted-by-ref" gotcha handled
  for you) then `$$encode^STDJSON` **and** auto-adds `Content-Type:
  application/json`. So "STDHTTPD adds Content-Type" is literally true only on
  the `RSP("json")` path (raw `RSP("body")` gets none). Same idiom as
  `setError^STDHTTPD`. All FHIR arrays use only contiguous index 1 (no gappy
  array → no `U-STDJSON-ENCODE`).
- **MRN = the DFN** (single-station). Grounded in the vdocs gold corpus: the
  **ICN is forbidden for direct access** ("Direct access to ICNs in the PATIENT
  file is not allowed", MPIF) and is registration/MPI-assigned (a DBS-added
  patient has none); **SSN (.09) is PII** and not a record locator; the **DFN is
  the natural local record key** (`^DPT` IEN), always present. See
  [[notional-dbia-not-a-blocker]] for the @icr posture — getPatient adds NO
  direct VistA call (all via VSLFS), so the **ICR registry stays empty/green**
  and the waterline holds (v→v VSLFS, v→m STDHTTPD/STDJSON; no direct `^DPT`).
- **FileMan external date → ISO** is done in VWEBR (`$$fmToIso`/`$$monNum`),
  because VSLFS `$$get` returns **external** values only (no flags/internal
  param) — `$$GET1^DIQ` default is external ("JAN 01, 1950", "MALE"/"FEMALE").
  `$$fmToIso` is order-independent (4N→year, 1-2N→day, 3-alpha→month) and
  degrades partial dates to `YYYY`/`YYYY-MM`; the day-before-year bug (day
  dropped when its capture required year-seen-first) was the TDD red→green fix.
- **foia-t12's PATIENT #2 is EMPTY** — `$order(^DPT(0))=""` (only the file
  header node; FOIA public builds are scrubbed of patient data). So a live
  real-#2-read 200 is impossible there, and **VSLFS `$$set` cannot seed #2**
  (it does single-field adds; #2 has multiple required identifiers). The
  known-DFN tests **discover an existing record read-only** (scan `$$exists^VSLFS`
  1..5000 — never direct `^DPT`; zero mutation, nothing to back out) and
  **soft-skip loudly on an empty #2**. So the 200 real-read is proven on **vehu**
  (populated YDB-VistA); foia proves routing/400/404 + the pure mapping units.

## Verification (all green, driver stack only)
- **bare YDB (m-test-engine, --chset m) + bare IRIS (m-test-iris): 38/38 each**
  (VWEBRTST 26 + VWEBLTST 12). Routing, 400 (dispatch + real socket), and the
  pure FHIR-mapping unit tests (splitName/gender/fmToIso) run here with no
  FileMan.
- **vehu (YDB-VistA, --chset m): VWEBRTST 35/35** — a real #2 read → 200 FHIR
  Patient via `$$dispatch` AND over a real socket, plus 404.
- **foia-t12 (IRIS-VistA, --namespace VISTA): VWEBRTST 27/27** — 404 live; 200 +
  socket-200 soft-skip on the empty #2; mapping units green.
- `make check` exit 0 (bare YDB): fmt / lint 0 / arch layer-v / 7 drift gates
  (seams 0, **icr 0**, citations 0, namespaces **5 routines**, msl-pin v0.12.2,
  engine-access, **check-kids deterministic ✓**) / full test suite 56/56.
- **KIDS** `kids/vweb.build.json` now ships 5 routines (added `VWEBR`),
  **patch 1→2**; `dist/kids/VWEB.kids` + `dist/namespace-registry.json`
  regenerated. (`VWEBRTST` is a test routine — correctly excluded from the ship
  list.)

## Coverage posture
`make check` does **not** include coverage (it's a separate target; v-web CI is
engine-free-gates-only). Bare-engine VWEBR coverage is ~48% because
`getPatient`'s FileMan body only executes on a live VistA, and the coverage
monitor does **not** collect over the live docker transport (vehu reports 0.0%).
So the FileMan read path is a **documented coverage exception** (same class as
VWEBL's infra-gated `run`/`launch`) — proven by the live vehu suite, not bare
coverage.

## Next — M6.5
`VWEBA` auth: bearer/introspection → DUZ/#200, applied as STDHTTPD middleware
(`$$use^STDHTTPD`) so `/Patient/:id` stops being open. Then M6.6 = the §9 TLS
smoke test. See [[m6.3-vweb-listener]] for the transport this sits on.
