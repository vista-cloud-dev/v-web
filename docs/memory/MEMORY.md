# v-web — per-repo memory index

One line per memory file. Content lives in the files, not here. v-web is the
**VistA Web Services** package (`VWEB*`): the inbound socket adapter that drives
the m-stdlib `STDHTTPD` server framework over a real socket. Layer `v`
(VistA-coupled), consumes m-stdlib (`STDHTTPD`/`STDHTTPMSG`/`STDNET`, MSL
v0.12.2) and v-stdlib (`VSLTASK`/`VSLCFG`/`VSLENV`) upward. See the org +
per-repo `CLAUDE.md`.

- [phase4-traffic-console](phase4-traffic-console.md) — VSL/MSL **Phase 4 / M3 increment 1 DONE** (2026-06-21, branch `phase4-traffic-console`): **VWEBT** — the RPC+HL7->S3 tap health console. `snapshot(.J)` pure-reads the VSL* tap instruments (`VSLTAP`/`VSLTAPHL`) into a STDJSON object; `health(.REQ,.RSP)` serves `GET /traffic/health` as a `text/event-stream` SSE snapshot (per-request; STDHTTPD has no chunking, EventSource reconnect paces it); registered behind VWEBA auth via `routes^VWEBR`. **YDB 125/125 + IRIS 125/125**, gates green. Gotcha re-confirmed: `parseRsp` preserves header case (`$$hdr^STDHTTPMSG`). Added VWEBT to the explicit `vweb.build.json` routine list + regen namespaces/kids. REMAINS: SPA panels, fidelity panel, operator toggle. Part of [[rpc-traffic-s3-streaming]].
- [m6.6-vweb-smoke](m6.6-vweb-smoke.md) — VSL/MSL **M6.6 DONE** (2026-06-18, branch `m6.6-tls-smoke`): the end-to-end smoke that **CLOSES the M6 capstone**. New **`VWEBE2ETST`** suite (6 tests, no production surface — composition + verification) drives the whole vertical over a REAL socket: open-path 200, tokenless **401** (through the LIVE registered chain), and **THE capstone — an AUTHENTICATED 200 FHIR Patient on the wire** (auth middleware + #200 bind + VSLFS #2 read + STDJSON, the leg never tested e2e); plus **403** unprovisioned and the **§9 TLS gap-loud** assertion (`$$listenTls`→`,U-VWEB-NOTLS,`, soft-skip the real HTTPS round-trip until cert+`XU*8.0*787`). **bare YDB/IRIS 15/15, vehu 20/20** (authenticated 200 live), **foia 16/16** (403 live; 200 soft-skips on scrubbed #2); `make check` **92/92** across 7 suites. **GOTCHAS:** STDHTTPD route table is INDEXED (`SRV("route",n,"pattern")`) not path-keyed; **`parseRsp` preserves response header CASE** (use `$$hdr^STDHTTPMSG`); **byte mode mandatory on YDB** — without `--chset m` VWEBATST aborts 0/0 (JWT/HMAC bytes), so added a **`CHSET` Makefile var** (default `m` for ydb). Decisions: **Q1** close M6 on the authenticated vertical over plaintext + loud TLS gate (real TLS = M2.T2 follow-up); **Q2** KIDS lifecycle + the v-pkg uninstall gap (#8989.51/#19 orphan) **documented, not run live**. M6.5 cross-repo OWED CLOSED (pin v0.13.0 + STDJWT). **M6 CAPSTONE CLOSED.**
- [m6.5-vweb-auth](m6.5-vweb-auth.md) — VSL/MSL **M6.5 DONE** (2026-06-17, branch `m6.5-auth` off `m6.4-fhir-patient`): **VWEBA** auth middleware closes the M6.4 route. `authn^VWEBA(.REQ,.RSP,.CTX)` (registered via `register^VWEBA`→`use^STDHTTPD` in `routes^VWEBR`): open-path allow-list (/healthz), extract Bearer, **provider seam `$$validate`** (jwt = `$$verify^STDJWT`; introspection/SAML future), bind subject→#200 IEN via `$$bind` (`secid`=`$$bySecid^VSLSEC` / `ien`=`$$user^VSLSEC`), set `DUZ`; **401** unauth (+WWW-Authenticate+OperationOutcome), **403** unprovisioned. The relying-party half of **validate-a-TOKEN-not-the-PIV-card** (token validation=`m`/STDJWT [[m6.5-stdjwt]]; #200 binding=`v`/VSLSEC). Config via `^VWEB("rtcfg")` runtime cache→XPAR→default (tests run NO-XPAR/no-install); **FAIL-CLOSED** on empty key; DUZ set-not-killed (failed auth short-circuits before the handler). `getPatient` stays byte-identical; VWEBRTST handler tests use `routesNoAuth` (strip `SRV("mw")`). VWEBA makes no direct VistA call (the `$text(GET^XPAR)`/`(GET1^DIQ)` PROBES carry @icr 2263/DBS). **Bare YDB+IRIS 77/77** (security guarantees); **vehu 23+35, foia 23+27** (full token→DUZ e2e + 403, live both engines). KIDS **VWEB*1.0*3** (+VWEBA, +6 auth XPAR params). All gates green. **GOTCHAS:** undefined suite-local label → silent 0/0 abort; bare-signing tests must skip live (else default `secid` map → 403 not 400). **OWED:** bump MSL pin→v0.13.0 +STDJWT seam once the user tags MSL v0.13.0 (today pinned v0.12.2, STDJWT consumed-but-unpinned, check-msl-pin SKIP-greens). **Next M6.6** = §9 TLS smoke test (M6 capstone close).
- [m6.4-vweb-fhir-patient](m6.4-vweb-fhir-patient.md) — VSL/MSL **M6.4 DONE** (2026-06-17, branch `m6.4-fhir-patient`): **VWEBR** router + the FHIR R4 **`GET /Patient/:id`** handler — the first real route on the M6.3 transport. `routes^VWEBR` owns the table (GET /healthz kept + GET /Patient/:id → `getPatient^VWEBR`); `healthRoutes^VWEBL` removed, VWEBL/VWEBLTST repointed. `getPatient` reads #2 (.01/.02/.03) via **VSLFS** (no direct `^DPT`), maps to minimal FHIR (id/identifier-MRN=DFN/name/gender/birthDate), serializes via the **`RSP("json")` STDJSON seam** (auto Content-Type), 404/400 → FHIR OperationOutcome. **MRN=DFN** (ICN direct-access forbidden, SSN=PII — vdocs). FileMan external date→ISO in `$$fmToIso`. **foia #2 is empty** (`$order(^DPT(0))=""`) so the live 200 read is proven on **vehu** (35/35); known-DFN tests discover an existing record read-only + soft-skip on empty #2. Green: bare YDB+IRIS 38/38, vehu 35/35, foia 27/27; `make check` 56/56; KIDS 5 routines (patch 1→2). Coverage = documented exception (FileMan body live-only; monitor doesn't collect over docker). **Next M6.5** = VWEBA auth.
- [m6.3-vweb-listener](m6.3-vweb-listener.md) — VSL/MSL **M6.3 DONE** (2026-06-17): the new **v-web** repo + **VWEBIO** (transport/device/TLS adapter) + **VWEBL** (listener launcher + serial accept→serve→close loop) + **VWEBCFG** (XPAR config) + **VWEBENV** (KIDS env-check). The socket→`$$serve^STDHTTPD`→write **vertical is GREEN end-to-end on BOTH engines (30/30 each)**; after the **MSL v0.12.0→v0.12.1 repin** (CRLF, GAP 1) and the **v0.12.1→v0.12.2 repin** (peer-closed read, GAP 2), every VWEBLTST serve test — including the client read-BACK (`tServeHealthOverSocket`) — runs green on IRIS too (**VWEBLTST 12/12 both engines**, no 0/0 abort); the static `readbackSafe()` IRIS gate is deleted. Both STDNET IRIS socket gaps closed (see [[stdnet-iris-crlf-rawread-gap]]). All engine-free gates green (fmt/lint/arch/4 drift gates/msl-pin **v0.12.2**/check-kids `VWEB*1.0*1`); ICR registry **empty** (v-web makes zero direct VistA calls — all via VSL*). HTTP-first, TLS-gap-loud (`,U-VWEB-NOTLS,`); D1 socket handoff = single-process serial (jobbed-worker concurrency deferred). **Repo live at github.com/vista-cloud-dev/v-web** (public, default `main`); M6.3 on branch `m6.3-vweb-listener`, the v0.12.1 repin on `repin-msl-v0.12.1`, the v0.12.2 repin on `repin-msl-v0.12.2`.
- [stdnet-iris-crlf-rawread-gap](stdnet-iris-crlf-rawread-gap.md) — **TWO STDNET IRIS gaps** found by the VWEB serve vertical (both m-stdlib, NOT v-web), **both now ✅ RESOLVED**. **GAP 1 (CRLF)** — `readIris` did a CR-terminated read that stripped CRLF (HTTP framing → 400); fixed in **MSL v0.12.1** (PR #18, byte-at-a-time `read *c:t`) and **v-web repinned v0.12.0→v0.12.1** (2026-06-17), lighting up the server-side IRIS serve tests. **GAP 2 (peer-closed read)** — `$$read^STDNET` on a **peer-CLOSED** socket on IRIS raised an uncatchable `<DSCON>`-class disconnect that **KILLED the job** (vs draining buffered bytes + EOF), so the client read-BACK couldn't run on IRIS; fixed in **MSL v0.12.2** (`readIris` drains-then-EOFs under an ObjectScript try/catch, STDNETTST 22/22 dual-engine) and **v-web repinned v0.12.1→v0.12.2** + **deleted the static `readbackSafe()` gate** (2026-06-17), so the read-back runs green on IRIS (VWEBLTST 12/12 both engines). **Repro + fix inside.**
