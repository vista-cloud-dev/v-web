# v-web — per-repo memory index

One line per memory file. Content lives in the files, not here. v-web is the
**VistA Web Services** package (`VWEB*`): the inbound socket adapter that drives
the m-stdlib `STDHTTPD` server framework over a real socket. Layer `v`
(VistA-coupled), consumes m-stdlib (`STDHTTPD`/`STDHTTPMSG`/`STDNET`, MSL
v0.12.1) and v-stdlib (`VSLTASK`/`VSLCFG`/`VSLENV`) upward. See the org +
per-repo `CLAUDE.md`.

- [m6.3-vweb-listener](m6.3-vweb-listener.md) — VSL/MSL **M6.3 DONE** (2026-06-17): the new **v-web** repo + **VWEBIO** (transport/device/TLS adapter) + **VWEBL** (listener launcher + serial accept→serve→close loop) + **VWEBCFG** (XPAR config) + **VWEBENV** (KIDS env-check). The socket→`$$serve^STDHTTPD`→write **vertical is GREEN end-to-end on YDB (30/30)**; on IRIS the socket adapter is fully GREEN (VWEBIO 14/14) and — after the **MSL v0.12.0→v0.12.1 repin** (CRLF fix, 2026-06-17) — the **server-side serve path is now GREEN on IRIS too (27/27)**; only the client read-BACK (`tServeHealthOverSocket`) stays statically skipped, blocked by a SECOND STDNET IRIS gap (peer-closed read kills the job — see [[stdnet-iris-crlf-rawread-gap]] GAP 2). All engine-free gates green (fmt/lint/arch/4 drift gates/msl-pin **v0.12.1**/check-kids `VWEB*1.0*1`); ICR registry **empty** (v-web makes zero direct VistA calls — all via VSL*). HTTP-first, TLS-gap-loud (`,U-VWEB-NOTLS,`); D1 socket handoff = single-process serial (jobbed-worker concurrency deferred). **Repo live at github.com/vista-cloud-dev/v-web** (public, default `main`); M6.3 on branch `m6.3-vweb-listener`, the v0.12.1 repin on branch `repin-msl-v0.12.1`.
- [stdnet-iris-crlf-rawread-gap](stdnet-iris-crlf-rawread-gap.md) — **TWO STDNET IRIS gaps** found by the VWEB serve vertical (both m-stdlib, NOT v-web). **GAP 1 (CRLF) ✅ RESOLVED** — `readIris` did a CR-terminated read that stripped CRLF (HTTP framing → 400); fixed in **MSL v0.12.1** (PR #18, byte-at-a-time `read *c:t`) and **v-web repinned v0.12.0→v0.12.1** (2026-06-17), lighting up the server-side IRIS serve tests. **GAP 2 ⛔ OPEN** — `$$read^STDNET` on a **peer-CLOSED** socket on IRIS raises an uncatchable `<DSCON>`-class disconnect that **KILLS the job** (vs draining buffered bytes + EOF), so the client read-BACK can't run on IRIS; can't be runtime-probed → v-web uses a static `readbackSafe()` gate. **Repro + fix sketch inside.** Closing GAP 2 is an **m-stdlib (STDNET) increment**, NOT v-web.
